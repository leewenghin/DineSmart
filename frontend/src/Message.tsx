// create a react component, can use a javascript class or component

//PascalCasting 
// Captitalize the first letter of every word in this p and C
function Message() {
    // JSX: JavaScript XML
    const name = 'Mosh';
    if(name)
    return <h1>Hello {name}</h1>;
}

export default Message;